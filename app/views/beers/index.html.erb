<% if current_user && current_user.admin? %>
  <%= render partial: "beers/form/beer_form", locals: { beer: @beer, header: "Add a New Beer" } %>
<% end %>

<div id="beers">
  <% @beers.each do |beer| %>
    <%= render partial: "beer_panel", locals: { beer: beer } %>
  <% end %>
</div>
